// export const selectSomething = (state: RootReducerType): SomeType => state.something.some


// Забираем конкретные значения из Стейта и экспортируем. Для каждого значения создаем отдельный файл

// В файле импорта будем писать: const some = userSelector(selectSomething)
export {}